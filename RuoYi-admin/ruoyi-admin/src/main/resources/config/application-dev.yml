# 数据源配置
spring:
  datasource:
    druid:
      master:
        url: jdbc:mysql://192.168.1.100:3306/ry?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8
        username: ry
        password: x6nt8hbp5dYWSB4K
      statViewServlet:
        login-username: admin
        login-password: admin
  data:
    redis:
      host: 192.168.1.100
      port: 6379
      password: redis@2025
  # 服务模块
  devtools:
    restart:
      # 热部署开关
      enabled: true

springdoc:
  swagger-ui:
    # 生产不能开启 swagger
    enabled: true

# 日志配置
logging:
  level:
    com.ruoyi: debug
    org.springframework: warn